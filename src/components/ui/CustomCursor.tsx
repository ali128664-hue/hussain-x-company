import React from 'react';

// Mouse animation removed per user request for fast, native OS cursor performance
export const CustomCursor: React.FC = () => {
  return null;
};

export default CustomCursor;
